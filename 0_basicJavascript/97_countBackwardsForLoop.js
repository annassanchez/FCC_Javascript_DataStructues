//you can also loop backwards with a for loop
//for example, we can loop backwards even numbers
const ourArray = [];

for (let i = 10; i > 0; i -= 2) {
  ourArray.push(i);
}

// Setup
const myArray = [];

// Only change code below this line
for (let i = 9; i > 0; i -= 2) {
  myArray.push(i);
}