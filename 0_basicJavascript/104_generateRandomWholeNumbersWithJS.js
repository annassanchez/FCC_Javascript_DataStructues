function randomWholeNum() {

    // Only change code below this line
    var result = 0;
      while (result === 0) {
        result = Math.floor(Math.random() * 10);;
      }
      return result;
  }