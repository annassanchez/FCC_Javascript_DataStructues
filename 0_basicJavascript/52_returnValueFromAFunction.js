//you can pass values into a function with arguments
//you can use a return to send a value back out of a function
function plusThree(num) {
    return num + 3;
  }
var answer = plusThree(5);
console.log(answer);
//create a function timesFive that accepts an argumnent, multiplies it by 5 and returns the new value
function timesFive(num) {
    return num * 5;
  }
var answer2 = timesFive(5);
console.log(answer2);