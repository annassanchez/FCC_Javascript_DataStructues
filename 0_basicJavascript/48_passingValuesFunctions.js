//a function can also include variables
function testFun(param1, param2) {
    console.log(param1, param2);
}
//example: create a function that accepts two arguments and outputs the sum to the dev console
function functionWithArgs(param1, param2) {
    console.log(param1 + param2);
}
functionWithArgs(1, 2);