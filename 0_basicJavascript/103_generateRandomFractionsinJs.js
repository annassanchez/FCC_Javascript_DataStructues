//Math.random() in js is a function that generates a random decimal number between 0 incluse and 1 exclusive
//this function can create random numbers
function randomFraction() {

    // Only change code below this line
  
    var result = 0;
    while (result === 0) {
      result = Math.random();
    }
    return result;
    // Only change code above this line
  }