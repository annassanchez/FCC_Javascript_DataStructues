//you can assign variables with an operation on the other side of the equal
//var ourSum = sum(5,12);
//console.log(ourSum);
//example
// Setup
var processed = 0;
function processArg(num) {
  return (num + 3) / 5;
}
// Only change code below this line
processed = processArg(7);
console.log(processed);