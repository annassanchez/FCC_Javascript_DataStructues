//parseInt() parses a string and returns an integer
const a = parseInt("007");
//use the parseInt in the convertInteger so it converts the input str to an integer
function convertToInteger(str) {
    return parseInt(str);
  }
  
  convertToInteger("56");